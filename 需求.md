# AD 自动化助手需求

> 优先级：**P0** = 最高（先做）｜**P1** = 中｜**P2** = 视情况
>
> 基础能力（已就绪）：`ad-ops` / `ad-blackbox-analysis` / `ad-check-analysis` 三个 skill。

---

## 🔒 架构原则（贯穿所有需求 · 不可妥协）

### 背景
生产环境实际使用的 LLM **能力较弱、幻觉率高**，无法承担分析、判断、生成等需要确定性的工作。

### 原则
**参考 `ad-check-analysis` 的模式**，把绝大多数业务逻辑**固化进脚本**：

| 角色 | 职责 |
|---|---|
| **脚本（Python）** | 数据获取、判断、分析、阈值、模板渲染、最终输出 —— 全部 |
| **LLM** | 仅做两件事：**① 选对脚本和参数 ② 把脚本输出原样展示给用户** |

### 必须遵守的实现约束

1. **不允许 LLM 做"分析"或"推断"**
   - 异常判定、阈值比较、根因关联，全部写在脚本里
   - 任何"看起来像不像异常"由脚本结论说了算，LLM 不二次解读
2. **不允许 LLM 拼装最终输出**
   - 脚本直接输出 Markdown（或 JSON），LLM 原样贴入对话，**不删不改不补**
   - 参考 `ad-check-analysis` 中 `render_markdown()` 直接产出整篇报告的做法
3. **数据来源严格闭环**
   - 每个需求都要明确"分析结果只来源于脚本读取的数据"
   - 禁止混入"模型常识"或跨需求的 API 调用结果
4. **脚本输出双形态**
   - 默认 Markdown（给对话展示）
   - 可选 `--format json`（给行里平台对接和后续自动化）
5. **错误也由脚本说**
   - 失败、超时、数据缺失，由脚本返回明确文案与非零退出码
   - LLM 不"善意补救"，原样转述

### 反例（明确禁止的做法）

| ❌ 错误模式 | ✅ 正确做法 |
|---|---|
| LLM 把脚本输出的 JSON 自己拼成表格 | 脚本直接输出 Markdown |
| LLM 看到 CPU 95% 自己写"建议扩容" | 建议项由脚本根据规则生成 |
| LLM 调用多个 API 后自己做交叉分析 | 由专门的脚本子命令做交叉分析 |
| LLM 把"约等于" / "看起来正常"加进结论 | 脚本输出确定结论，LLM 不修饰 |

### 对每个需求的具体含义

- **需求 1**：脚本直出"设备总览" Markdown，LLM 不参与拼装
- **需求 2**：异常判定规则、阈值、时间窗、根因关联全部写在脚本里
- **需求 3**：配置脚本由生成器脚本产出，LLM 不写配置内容
- **需求 4**：校验规则脚本化，LLM 不做"看起来对不对"的判断

---

## P0 · 需求 1：配置 / 状态 / 流量 / 证书 查询

### 目标
对 AD 设备做只读巡查，结果在**对话里清晰可视化**呈现；同时为**后续对接行里平台**预留机器可读输出（JSON）。

### 查询范围

| 类别 | 内容 | 数据来源（现状） |
|---|---|---|
| 配置 | VS 地址、端口、所属 Pool、节点列表、节点健康状态 | `ad-ops`: `get_virtual_services` / `get_pools` |
| 状态 | HA 状态、SSH 状态、模块状态 | `ad-ops`: `get_ha_status` / `get_ssh_config` |
| 流量 | 新建 / 并发 / 上下行吞吐 | `ad-ops`: `get_vs_stat` / `get_vs_trend` |
| 证书 | SSL 证书到期时间 | `ad-ops`: `get_ssl_certificates` 的 `validity_not_after` |

### 输入 / 输出
- **输入**：设备 IP + 凭据
- **输出**（**由脚本直接产出，LLM 原样展示**）：
  - 对话视图：脚本渲染好的 Markdown 表格，关键字段（异常项、临期证书）显著标注
  - 平台对接：`--format json` 输出结构化数据（行里平台格式后续对齐）

### 待补充能力
- "**单台设备总览快照**"高层命令：一条命令拿到 配置 + 状态 + 证书 全貌（当前需多次调用拼装）
- 证书到期分级提醒（30 / 60 / 90 天阈值） // TODO 阈值与你确认

### 验收
- [ ] 一条命令拉出一台设备总览
- [ ] 临期证书自动高亮
- [ ] JSON / Markdown 双输出

---

## P0 · 需求 2：感知分析

### 目标
对单台 AD 设备做**异常感知**，覆盖 流量 / 状态 / 日志 / 地址冲突 四个维度。

### 核心痛点
**现 WEB 控制台 7 天颗粒度过粗，短时尖峰/突降被平均掉看不见。** 本需求第一要解决的就是"看得到细节"。

---

### 2.1 流量趋势分析（首先聚焦）

| 项 | 说明 |
|---|---|
| 视角 | 单个 VS |
| 指标 | 新建连接、并发连接、吞吐（上行/下行/总） |
| 颗粒度要求 | **必须细于 WEB**：7 天数据按小时甚至更细，不被聚合平均 |
| 异常定义 | 异常**增加** 或 异常**下降**（短时尖峰 / 突降 / 持续偏离） // TODO 量化口径与你确认 |

**关键技术待确认**：`ad-ops` 现有 `get_vs_trend(name, item, trend)` 是否能取到原始颗粒度？若不能，需改用 `get_vs_stat` 持续采样自存。

### 2.2 设备状态分析

| 指标 | 关注点 |
|---|---|
| CPU / 内存 / 硬盘 | 异常上升、异常下降 |
| 模块状态 | 模块异常（停止、重启、报错） |

数据源：`ad-ops` 的 `get_sys_system()` + `ad-check-analysis` 巡检报告 `ad.json` 中的硬件字段。

### 2.3 日志分析

数据源：`ad-blackbox-analysis` 导出的审计日志 + 系统日志。
用途：辅助流量 / 状态异常的**根因定位**（谁在什么时间改了什么）。

### 2.4 地址冲突检测

- VS 间 IP:Port 是否重叠
- Pool 节点是否重复

### 输出
**由脚本直接产出 Markdown，LLM 原样展示。** 内容包含：异常项列表 + 触发时间点 + 关联日志线索 + 可读的"为什么判定为异常"说明（说明由脚本根据预置规则生成，**不是 LLM 解读**）。

### 验收
- [ ] 拿到比 WEB 更细的流量颗粒度
- [ ] 能标出类似"过去 7 天 VS X 在 03:15 并发突增 3x"的事件
- [ ] CPU / 内存 / 硬盘异常能与日志事件对齐时间点

---

## P1 · 需求 3：配置脚本自动生成

### 目标
按行里设备**现状**，自动生成新增配置脚本，**生成前做幂等检查**，避免：
- 引用不存在的依赖（如 XFF 策略）
- 重复创建已存在的对象导致 API 报错

### 典型场景
新建 VS，要求"插入 XFF"：
1. 查询设备已有 XFF 策略
2. **存在** → 脚本引用现有策略名
3. **不存在** → 脚本先创建策略，再引用

### 输入
- 目标设备
- 需求描述（VS 名 / IP / 端口 / Pool / 附加能力如 XFF、SSL 卸载、健康检查等）

### 输出
**由生成器脚本直接产出**：
- 完整可执行的配置脚本（命令序列或 REST 调用序列） // TODO 选哪种载体与你确认
- 附幂等检查注释：哪些已存在、哪些待新建
- LLM **不参与**配置内容生成，仅负责调用生成器并展示结果

### 涉及组件
- 复用 `ad-ops` 做前置查询
- 新增：配置模板库 + 依赖解析模块

---

## P2 · 需求 4：配置审核（条件性）

### 目标
对**人工提交**的配置做：
- 语法 / 格式校验
- 功能是否符合预期
- 与现网配置是否冲突

### 优先级说明
**若需求 3 落地到位**，配置由生成器产出（等价于已审核），本需求**强度大幅下降**。保留作为兜底，覆盖人工旁路提交的情况。

### 涉及组件
- 复用需求 3 的依赖解析与冲突检测
- 复用需求 2.4 的地址冲突检测

---

## 实施顺序建议

1. **现状**：三个基础 skill 已就绪
2. **下一步（P0）**：
   - 给 `ad-ops` 加"设备总览快照"高层命令 → 服务需求 1
   - 验证 `get_vs_trend` 最细颗粒度，必要时改用 `get_vs_stat` 直采 → 服务需求 2.1
3. **再下一步（P1）**：配置生成器骨架，先支持"VS + XFF"一个场景
4. **后续（P2）**：配置审核器，视需求 3 的覆盖度决定是否投入

---

## 待确认问题（TODO）

1. **平台对接的输出格式**：行里平台对 JSON 结构有偏好吗（OpenAPI / 自定义）？
2. **流量异常量化口径**：用同比、环比、还是滑动均值偏离？阈值定多少？
3. **证书到期分级阈值**：30/60/90 天合理吗？
4. **配置脚本载体**：CLI 命令脚本 / REST 调用列表 / Ansible playbook，选哪种？
5. **多设备视角**：所有功能是否都需要支持"批量跨设备"？（呼应之前讨论的 `ad-check-analysis` 多设备方案）
